info:
  name: INTROSPECTION
  type: http
  seq: 3

http:
  method: POST
  url: "{{local_base_url}}/auth/token/introspect/"
  body:
    type: form-urlencoded
    data:
      - name: identifier
        value: "{{usermail}}"
        disabled: true
      - name: identifier
        value: "+528148723812"
        disabled: true
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
