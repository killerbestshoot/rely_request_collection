info:
  name: LOGOUT
  type: http
  seq: 18

http:
  method: POST
  url: "{{local_base_url}}/identity/auth/logout/"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
